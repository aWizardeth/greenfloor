{
  "name": "greenfloor-webui",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others -n \"api,ui\" -c \"green,cyan\" \"node scripts/dev-api.js\" \"vite --port 3000\"",
    "dev:ui": "vite --port 3000",
    "dev:api": "node scripts/dev-api.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vite": "^6.2.0"
  }
}
